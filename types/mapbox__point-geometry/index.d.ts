/** Stub for deprecated @types/mapbox__point-geometry; @mapbox/point-geometry provides its own types. */
declare module '@mapbox/point-geometry' {
  export class Point {
    x: number;
    y: number;
    constructor(x: number, y: number);
  }
}
